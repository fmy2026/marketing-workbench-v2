# P01 / P03 创建字段对比

本文件只保存脱敏结构结论，不保存 raw payload、raw response、完整触点 URL 或凭据。

## 固定对象

- P03 job：`JOB-MWBV2-20260824092327-494BF1`
- P03 payload_hash 匹配：`true`
- P03 create actions：`1`
- P03 created objects：`0`
- P01 readonly status：`passed`，api_code：`0`，request_id_present：`true`
- 旧项目 P01 create payload：`legacy_raw_create_payload_not_retained`

## 最终回答

1. P03 与 P01 是否存在同名重复：否；P01 项目存在且已按 project_id/name 命中，但 P03 项目名不同，P03 不是 P01 同名重复。P03 项目名是 P03，不是 P01 名称。
2. P03 是否存在 P01 有、但 P03 缺失的关键创建字段：否；按 v2 当前官方/旧脚本抽象出的关键创建字段，P03 均 present。
3. P03 是否存在 P01 没有、且旧脚本/官方文档不允许的字段：否；P03 字段均在官方或旧成功脚本抽象出的允许创建字段范围内。
4. 是否发现字段类型、枚举、素材、品牌、事件、DMP 或触点结构差异：未发现可证实创建字段合同差异；P01 list 与 P03 create 的 response/request 差异不直接作为根因，素材、品牌、事件、DMP、触点在 P03 v2 结构中均 present。
5. P03 的 40000 是否已有明确根因：仍需下一次新 job 的 safe error summary 才能确认。

## 结构摘要

- P01 平台字段数：116
- P03 v2 字段数：71
- 旧 P01 创建字段数：0
- P01 list-only 字段不用于判定 P03 创建缺字段：74
- P03 create-only 字段不用于判定 P03 非法字段：29
- P03 缺关键创建字段数：0
- P03 非允许创建字段数：0
- P03 payload hash：sha256:152babf25efa31d4aa526d17a5dd7379f687dc8a069e5e93bf51eb38aa73a2f4
- P03 payload hash matches target：true

## 边界

- 未重试 P03。
- 未调用 `std_project/create`。
- 未新建 runtime job。
- 未刷新 token。
- 未保存 raw JSON。
