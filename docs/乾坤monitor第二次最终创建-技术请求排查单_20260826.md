# 乾坤 monitor 第二次最终创建技术请求排查单

更新时间：2026-08-26 CST

## 背景

本次针对新账户执行第二次且最后一次乾坤 monitor 真实创建。

| 项 | 值 |
| --- | --- |
| 乾坤环境 | 乾坤后台生产环境，仓库版本不记录完整域名 |
| 路线 | `oceanengine_3_byte_mini_game` |
| 游戏 | `JSZC` |
| 广告账户 ID | `1871922346964041` |
| provision_id | `MPR-OCEANENGINE-3-BYTE-MINI-GAME-JSZC-1871922346964041` |
| 历史参考 monitor_id | `245791` |
| 本次 attempt_no | `2` |
| 最大允许次数 | `2` |
| 当前最终状态 | `terminal_failed` |

说明：本项目文件不保存 token、Cookie、raw request、raw response、完整触点 URL。本文档列出完整业务参数、接口返回摘要、hash 和回查结果，便于技术按服务端日志定位。

## 调用顺序

| 顺序 | 接口 | 目的 | 结果 |
| --- | --- | --- | --- |
| 1 | `POST /tf/account_info/accountIndex` | 确认广告账户与乾坤内部账户记录 | 通过 |
| 2 | `POST /tf/ad/index` | 创建前精确查重 | 通过，0 条 |
| 3 | `POST /tf/ad/monitorSerialNumberAdd` | 第二次且最后一次创建 monitor | 返回业务码 `500` |
| 4 | `POST /tf/ad/index` | 创建后精确回查 | 通过，0 条 |

## 公共请求头

| Header | 值 |
| --- | --- |
| `Accept` | `application/json` |
| `Content-Type` | `application/x-www-form-urlencoded` |
| `X-Passport-Token` | 已正确传入，具体值不写入仓库 |

请求体格式：`application/x-www-form-urlencoded`。

## 1. accountIndex 账户确认

接口：

```text
POST /tf/account_info/accountIndex
```

请求参数：

| 参数 | 值 |
| --- | --- |
| `accountId` | `1871922346964041` |
| `pageNo` | `1` |
| `pageSize` | `10` |

返回摘要：

| 字段 | 值 |
| --- | --- |
| status | `passed` |
| httpStatus | `200` |
| apiCode | `0` |
| apiMessage | `Success` |
| exactMatchCount | `1` |
| accountId | `1871922346964041` |
| technicalAccountRecordId | `8448` |
| mediaAccountId | `8448` |
| agentId | `613` |
| agentName | `北京国新汇金股份有限公司` |
| owner | `fengmeiyu` |
| ownerName | `冯美钰` |
| authStatus | `ready` |
| authStatusName | `授权正常` |
| platformStatus | `1` |
| advertiserNamePresent | `true` |
| accessTokenPresent | `true` |

## 2. 创建前 monitor 查重

接口：

```text
POST /tf/ad/index
```

实际下发查询参数：

| 参数 | 值 |
| --- | --- |
| `pageNo` | `1` |
| `pageSize` | `50` |
| `mediaAccountId` | `1871922346964041` |
| `ssoOwner[]` | `fengmeiyu` |
| `packageId` | `36820` |
| `os[]` | `3` |
| `channel[]` | `dymini3k` |
| `monitorApi[]` | `toutiao_wxgame` |
| `usage` | `0` |

v2 返回后参与精确匹配的字段：

| 字段 | 目标值 |
| --- | --- |
| `package_id` / `packageId` | `36820` |
| `channel` | `dymini3k` |
| `media_id` / `mediaId` | `310` |
| `agent_id` / `agentId` | `613` |
| `monitor_api` / `monitorApi` | `toutiao_wxgame` |

返回摘要：

| 字段 | 值 |
| --- | --- |
| status | `passed` |
| httpStatus | `200` |
| apiCode | `0` |
| apiMessage | `Success` |
| resultTotal | `0` |
| rowCount | `0` |
| exactMatchCount | `0` |
| responseHash | `sha256:deb578e6c2dac77216d930bfb38d5f595ca2a132f56700e5716d83aafbd70493` |

## 3. monitorSerialNumberAdd 创建请求

接口：

```text
POST /tf/ad/monitorSerialNumberAdd
```

实际创建参数：

| 参数 | 类型 | 值 | 说明 |
| --- | --- | --- | --- |
| `os` | int | `3` | 系统：微信小游戏 |
| `package_id` | string | `36820` | 融合拿包 ID |
| `cate_id` | int/string | `122` | 游戏组 ID |
| `vest_id` | int/string | `1414` | 马甲 ID |
| `channel` | string | `dymini3k` | 融合渠道 |
| `owner` | string | `fengmeiyu` | 数据归属人 SSO |
| `media_id` | int/string | `310` | 第三层媒体资源位，人工确认：通投智选（原生竞价） |
| `agent_id` | int/string | `613` | 代理 ID |
| `num` | int | `1` | 生成监测地址数量 |
| `usage` | int | `0` | 用途：普通广告 |
| `monitor_api` | string | `toutiao_wxgame` | 监测 API 类型 |
| `media_account_id` | int/string | `8448` | 乾坤内部投放账号记录 ID |
| `remark` | string | `mwbv2-JSZC-1871922346964041` | v2 创建备注 |

未传参数：

| 参数 | 原因 |
| --- | --- |
| `package_download_url` | 当前渠道不是 `3k/apple3k` 包下载地址必填场景 |
| `server_callback_param_list` | 当前未启用服务端上报参数 |
| `server_callback_type` | 当前未启用服务端上报模式 |
| `server_callback_data_types` | 当前未启用服务端上报数据类型 |
| `multiplex_monitor_id` | 当前不是复用计划创建 |

请求字段 manifest：

```text
agent_id
cate_id
channel
media_account_id
media_id
monitor_api
num
os
owner
package_id
remark
usage
vest_id
```

字段完整性：

| 项 | 值 |
| --- | --- |
| requiredFieldsPresent | `true` |
| requestHash | `sha256:74993013f10e42d83b82547fbc5587a95f256eb4bd6a5acd7a0da5e42c00a174` |
| rawRequestStored | `false` |

创建接口返回摘要：

| 字段 | 值 |
| --- | --- |
| client status | `blocked` |
| httpStatus | `200` |
| apiCode | `500` |
| apiMessage | `服务器繁忙，请稍后重试(400)` |
| responseHash | `sha256:a85b2886b658ea6421161c5c1583dde35d4863025ad732278be57e56992be719` |
| rawResponseStored | `false` |

按乾坤接口文档，成功响应预期形态：

```json
{
  "code": 0,
  "msg": "success",
  "data": {}
}
```

本次实际业务返回为非成功：`apiCode=500`，`apiMessage=服务器繁忙，请稍后重试(400)`。

## 4. 创建后 monitor 回查

接口：

```text
POST /tf/ad/index
```

实际下发查询参数与创建前查重一致：

| 参数 | 值 |
| --- | --- |
| `pageNo` | `1` |
| `pageSize` | `50` |
| `mediaAccountId` | `1871922346964041` |
| `ssoOwner[]` | `fengmeiyu` |
| `packageId` | `36820` |
| `os[]` | `3` |
| `channel[]` | `dymini3k` |
| `monitorApi[]` | `toutiao_wxgame` |
| `usage` | `0` |

返回摘要：

| 字段 | 值 |
| --- | --- |
| status | `passed` |
| httpStatus | `200` |
| apiCode | `0` |
| apiMessage | `Success` |
| resultTotal | `0` |
| rowCount | `0` |
| exactMatchCount | `0` |
| responseHash | `sha256:deb578e6c2dac77216d930bfb38d5f595ca2a132f56700e5716d83aafbd70493` |

## 关联确认事实

| 维度 | 值 | 来源 |
| --- | --- | --- |
| L3 media_id | `310` | 历史 monitor `245791` + 用户乾坤后台人工确认 |
| L3 media_name | `通投智选（原生竞价）` | 历史 monitor `245791` + 用户乾坤后台人工确认 |
| monitor_api | `toutiao_wxgame` | 历史 monitor `245791` + 本次人工覆盖 |
| agent_id | `613` | `accountIndex` + 历史 monitor `245791` |
| account record | `8448` | `accountIndex` 新账户唯一命中 |
| usage | `0 = 普通广告` | `/ajax/selectList/getList type=usageList` |

人工 L3 覆盖 evidence：

```text
EV-QK-MANUAL-L3-CONFIRM-OCEANENGINE-3-BYTE-MINI-GAME-JSZC-1871922346964041-310
```

本次 ensure evidence：

```text
EV-MPR-OCEANENGINE-3-BYTE-MINI-GAME-JSZC-1871922346964041-ENSURE
```

## 数据库最终状态

`mwb.monitor_provision_runs`：

| 字段 | 值 |
| --- | --- |
| `provision_id` | `MPR-OCEANENGINE-3-BYTE-MINI-GAME-JSZC-1871922346964041` |
| `status` | `terminal_failed` |
| `monitor_serial_id` | 空 |
| `monitor_id` | 空 |
| `create_called` | `true` |
| `create_attempt_no` | `2` |
| `error_summary` | `monitor_create_busy_retry_exhausted` |
| `evidence_artifact_id` | `EV-MPR-OCEANENGINE-3-BYTE-MINI-GAME-JSZC-1871922346964041-ENSURE` |

`mwb.monitor_provision_attempts`：

| attempt_no | trigger_reason | attempt_status | http_status | api_code | error_category | error_summary |
| --- | --- | --- | --- | --- | --- | --- |
| `1` | `initial_create_once` | `failed` | 空 | `500` | `server_busy` | `monitor_create_failed:500:服务器繁忙，请稍后重试(400);post_create_monitor_readback_missing` |
| `2` | `server_busy_retry` | `failed` | `200` | `500` | `server_busy` | `monitor_create_failed:500:服务器繁忙，请稍后重试(400)` |

## 排查判断

v2 侧判断：

- 创建接口必填字段已齐：`requiredFieldsPresent=true`。
- 创建前查重为 0 条，说明不是 v2 查重命中后跳过。
- 创建接口实际被调用，返回 HTTP 200 但业务码 500。
- 创建后回查仍为 0 条，说明本次没有可回收的 monitor_id。
- attempt_count 已到 2，系统已禁止第三次创建。

建议技术重点排查：

- 服务端日志按 `requestHash=sha256:74993013f10e42d83b82547fbc5587a95f256eb4bd6a5acd7a0da5e42c00a174`、时间 `2026-08-26 19:25 CST` 附近定位。
- 检查 `media_id=310`、`media_account_id=8448`、`agent_id=613`、`monitor_api=toutiao_wxgame` 在乾坤服务端创建链路中是否存在权限、映射或资源位可用性异常。
- 检查同一服务端错误是否也影响 `/tf/ad/changeMediaId`，此前该接口对 `media_id=310` 也返回 `500 服务器繁忙，请稍后重试(400)`。
- 如果后台人工处理后生成 monitor，请只通过 `/tf/ad/index` 回查入库，不要再调用 `/tf/ad/monitorSerialNumberAdd`。
