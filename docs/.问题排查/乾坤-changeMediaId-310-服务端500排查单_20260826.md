# 乾坤 changeMediaId(310) 服务端 500 排查单

更新时间：2026-08-26 CST

## 背景

v2 当前为 `oceanengine_3_byte_mini_game / JSZC / advertiser_id=1871922346964041` 补齐乾坤监测序号创建前的只读技术组合。包链路和账户入口已通过，只剩 L3 媒体资源位到目标账户与监测 API 的可选性未通过。

## 目标资源位

| 项 | 值 |
| --- | --- |
| `os` | `3` |
| `media_id` | `310` |
| 人工确认资源位名称 | `通投智选（原生竞价）` |
| 关联账户记录 | `8448` |
| 期望 `monitor_api` | `toutiao_wxgame` |
| 期望 `agent_id` | `613` |

说明：`os=3` 在 v2 中按乾坤“小游戏”技术系统类型理解；最终业务语义由 `platform / L2 媒体 / L3 media_id / monitor_api / channel` 共同决定。

## 异常接口

| 项 | 内容 |
| --- | --- |
| 接口 | `POST /tf/ad/changeMediaId` |
| 请求格式 | `application/x-www-form-urlencoded` |
| 参数 | `os=3`、`media_id=310` |
| 调用次数 | 2 次独立受控调用 |
| 两次返回 | `500 / 服务器繁忙，请稍后重试(400)` |
| raw request / raw response | 未保存 |
| token / Cookie / 完整请求头 | 未写入项目文件 |

## 两次调用证据

| 证据 | 时间 | 请求指纹 | 响应 hash | 结果 |
| --- | --- | --- | --- | --- |
| `EV-QK-LEVEL3-MEDIA-RESOURCE-OCEANENGINE-3-BYTE-MINI-GAME-JSZC-1871922346964041-310` | `2026-08-26 17:49:56 CST` | `sha256:ff0ce171f052f7d2c2c8acc859c0e61c720a3a3327efa4b3b2fd20f5939c761b` | `sha256:a85b2886b658ea6421161c5c1583dde35d4863025ad732278be57e56992be719` | `500 / 服务器繁忙，请稍后重试(400)` |
| `EV-QK-LEVEL3-MEDIA-RESOURCE-OCEANENGINE-3-BYTE-MINI-GAME-JSZC-1871922346964041-310-R02` | `2026-08-26 18:18:44 CST` | `sha256:ff0ce171f052f7d2c2c8acc859c0e61c720a3a3327efa4b3b2fd20f5939c761b` | `sha256:a85b2886b658ea6421161c5c1583dde35d4863025ad732278be57e56992be719` | `500 / 服务器繁忙，请稍后重试(400)` |

## 对照接口

| 对照项 | 结果 |
| --- | --- |
| `accountIndex` | 同一凭据下成功，唯一命中 `advertiser_id=1871922346964041` |
| 历史 `/tf/ad/index` | `monitor_id=245791` 可读，历史行包含 `media_id=310` |
| 历史行字段 | `package_id=36820`、`vest_id=1414`、`channel=dymini3k`、`agent_id=613`、`monitor_api=toutiao_wxgame` |
| `selectList(type=mediaList)` | 同一凭据下成功，返回 `177` 项 |
| 目录对照 | 未命中 `value=310`，未命中名称 `通投智选（原生竞价）` |

`selectList(type=mediaList)` 只作为目录观察证据：目录不命中不能推翻后台和历史 monitor 中的 L3 资源位事实，也不能替代 `changeMediaId` 对账户与 `monitor_api` 可选性的验证。

## 当前影响

- 未获得 `changeMediaId(310)` 返回的 `accountIdList`。
- 未获得 `changeMediaId(310)` 返回的 `monitorApiList`。
- 未调用 `changeMediaAccountId(8448)`。
- 未写入 `media_resource_to_allowed_account_record`、`media_resource_to_allowed_monitor_api`、`account_record_to_agent` 关系。
- 未创建新的监测序号。

## 请技术侧确认

1. `POST /tf/ad/changeMediaId` 当前是否存在服务端异常或权限侧异常。
2. `os=3 + media_id=310` 当前是否仍支持返回账户记录与监测 API 列表。
3. `310 / 通投智选（原生竞价）` 是否应出现在 `selectList(type=mediaList)` 目录中；若不应出现，请确认该目录与 L3 资源位目录的差异。
4. 若 `310` 已不可用，请提供当前可用于 `os=3 + toutiao_wxgame + dymini3k` 的 L3 `media_id`。
5. 若接口需要额外参数，请明确参数名、类型、是否必填、示例值。

## v2 当前下一步

等待乾坤技术侧确认 `changeMediaId(310)` 的服务端 500 根因，或提供当前有效 L3 `media_id`。在此之前，v2 不会发起第二个监测序号创建。
