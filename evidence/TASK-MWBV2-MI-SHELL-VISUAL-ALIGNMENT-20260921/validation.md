# 市场情报视觉壳层对齐验证

验证时间：2026-09-21T07:09:55Z。

| 验收 | 方法与结果 | 证据 |
| --- | --- | --- |
| AC-01 | Chromium 在 1280×800 同一视口读取市场情报与投放执行当前页面的计算样式和边界。两页顶栏均为 58px，侧栏均为 216px；标题均从 `(244, 90)` 开始；输入框均为 `(244, 728, 958×40)`。市场情报输入文字与发送图标均为 16px，对齐投放执行实际渲染。 | Chromium 计算样式与边界读取记录 |
| AC-02 | Chromium 在 560×700 视口下验证市场情报侧栏 `display:none`；输入框边界为 `left=14, right=496, bottom=684`，发送按钮右边界为 546，均在视口内。投放执行紧凑壳层的输入区域同样在视口内。 | Chromium 计算样式与边界读取记录 |
| AC-03 | `node tests/market-intelligence.test.mjs` 通过 129 项隔离浏览器与功能检查，`externalRequests=0`；`git diff --check` 与任务 start 检查通过。关闭与远端 SHA 核验在终态记录。 | 命令输出、本 Task Manifest |

市场情报隔离预览的投放执行业务接口缺少测试夹具，因此对照只将当前投放执行页面的工作区壳层显示为静态 DOM；未调用平台或业务写入接口。
