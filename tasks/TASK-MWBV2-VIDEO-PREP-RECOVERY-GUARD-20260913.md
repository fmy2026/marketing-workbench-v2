# TASK-MWBV2-VIDEO-PREP-RECOVERY-GUARD-20260913

任务状态、读取清单与验证结果：[Context Manifest](../tasks-context-manifests/TASK-MWBV2-VIDEO-PREP-RECOVERY-GUARD-20260913.json)。

## 目标

保留三阶段七 Node、既有 Plan 与单次确认链路，修复视频资源未能形成精确绑定计划时仍可能生成默认写动作的缺口，并让工作台明确展示视频卡点与既有只读恢复入口。

## 批准方案

用户已批准“通用资源准备与卡点恢复最小修正方案”：视频准备合同成为绑定集合、批次、hash、可执行性和阻断的唯一来源；Plan 编译、确认前校验和执行器复用该合同。来源不完整、歧义或状态不明时 fail-closed，不再回退默认一次调用。工作台使用现有文字命令链新增“重新只读准备”按钮，不增加 API、Gate、Node、Plan 类型或账户特例。

## 范围

- 修正通用视频准备合同、资源 Plan 调用量编译与 confirmation 前 drift 校验；
- 让 Node 04 在引导视频依赖未满足时仍完成普通视频只读核验；
- 补充视频 root blocker 表述、确定性恢复提示和工作台恢复按钮；
- 增补契约、Plan、会话和进度 smoke，并回写当前机制文档。

## 非目标

- 不执行真实平台写入、旧 Plan 重放、旧 Case 状态篡改或历史数据回填；
- 不新增数据库迁移、Node、Gate、Plan 类型、确认步骤或账户/Case 专用逻辑；
- 不修改已修复的 `materialSourceResources` 装载机制。

## 验收

- AC-01: 缺失、重复、未验证或未探测的视频来源不能编译或确认视频写动作；已就绪为零动作，部分缺失仅冻结精确绑定集合和批次。
- AC-02: confirmation 前复核视频准备合同、批次 hash 与调用上限；任一漂移在确认占用和平台写入前停止。
- AC-03: Node 04 的普通视频核验不因引导视频依赖等待而跳过；现有七节点顺序与确认链路保持不变。
- AC-04: 具体视频卡点在服务端和页面可见；账户本人可通过按钮调用既有“重新只读准备”，同一失败运行只创建或复用一个 fresh Job。
- AC-05: 相关 smoke、项目合同检查、静态检查通过，且机制文档反映最终行为。

## 停止条件

- 需要新增平台写入、重试策略、数据库 Schema、Node、Gate 或确认机制；
- 需要使用账户、Case、Job 或用户 ID 作为运行时分支；
- 验证发现现有通用合同无法表达所需安全边界。

## 交付说明

完成后记录 Task 验收证据并关闭当前指针。真实账户仅由本人在工作台执行既有“重新只读准备”；本开发 Task 不触发真实资源准备或项目创建。
