# TASK-MWBV2-AGENT-HUB-MULTI-MODULE-PREVIEW-20260913

任务状态、读取清单与验证结果：[Context Manifest](../tasks-context-manifests/TASK-MWBV2-AGENT-HUB-MULTI-MODULE-PREVIEW-20260913.json)。

## 目标

在数字员工广场明确市场情报、投放策略与投放创建三者的职责，并将前两者展示为不可进入工作区的预告模块。

## 批准方案

按用户于 2026-09-13 批准的“三个 Agent 的定位与广场展示最小方案”实施：保留投放创建的服务端注册、真实能力统计和入口；将通用“即将上线”占位卡替换为“市场情报”和“投放策略”两张前端静态预告卡。广场副标题说明三者衔接关系；预告模块不注册为可用 Agent，不提供工作区、模型配置、情报采集、策略生成或 Agent 间调用。

## 范围

- 修改广场渲染、页面文案及必要样式，并扩展 Agent Hub smoke 覆盖卡片展示和不可进入的边界。
- 更新方案和工作台逻辑文档，说明当前三个模块的职责与可用性。
- 仅修改 Manifest 所列路径；不修改数据库、服务端 Agent 注册表、工作区路由或投放执行内核。

## 非目标

- 不实现市场情报或投放策略的工作区、模型配置、数据采集、策略生成或模块间调用。
- 不改变 `/api/agents` 的公开目录、投放创建的七节点统计、模型配置或既有入口。
- 不执行真实平台写入、OAuth 刷新、预算或出价修改。

## 验收

- AC-01: 广场展示投放创建、市场情报、投放策略三张卡片，文案、顺序、状态和按钮符合批准方案。
- AC-02: 两张预告卡没有入口行为或能力统计；投放创建原入口、真实能力统计与工作区不受影响。
- AC-03: 桌面三列、窄屏两列和手机单列布局下，三张卡片无溢出且按钮对齐。
- AC-04: Agent Hub 回归和项目合同检查通过，未引入服务端注册、数据库或平台写入变更。

## 停止条件

- 完成展示需要注册新可用 Agent、创建工作区或修改服务端接口时停止并重新确认范围。
- 实现需要修改数据库、投放执行、模型配置或真实平台权限时停止。
- 既有 Agent Hub 回归无法在不扩大范围的情况下保持通过时停止并报告。

## 交付说明

完成后记录前端预告卡、文案、响应式样式、回归结果与文档依据；动态账户、Case、Job 和平台事实不写入本 Task。
