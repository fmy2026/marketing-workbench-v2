# TASK-MWBV2-PROJECT-VIDEO-APPEND-20260914

任务状态、读取清单与验证结果：[Context Manifest](../tasks-context-manifests/TASK-MWBV2-PROJECT-VIDEO-APPEND-20260914.json)。

## 目标

在投放创建工作区支持为已有项目追加一至一百个已核验视频，并复用本人确认、单次执行和权威回查安全链。

## 批准方案

实施用户批准的“追加 1–100 个视频，按事项展示流程”方案：乾坤视频标识码经只读核验映射为目标账户可用视频；物料户已存在而目标户缺失时先生成素材推送 Plan，追加使用项目素材专用接口；物料户缺失只形成准备 blocker。

## 范围

- 统一请求、Case/Job、Plan/action/readback、必要迁移和工作台展示。
- 追加事项的项目推荐、素材状态和两类受控 Plan。
- 隔离 HTTP、浏览器和模拟平台验证。

## 非目标

- 首次上传、删除素材、修改项目其他配置或真实账户写入。
- 修改既有新建项目合同和创建重投例外。

## 验收

- AC-01: 支持 v2 追加请求的自然语言和 JSON 校验，1–100 个标识码无截断。
- AC-02: 三类素材可用性、项目推荐与跨账户边界均受控。
- AC-03: 推送与追加分别生成单次确认 Plan，并完成模拟回查。
- AC-04: 创建和追加在浏览器中均显示按事项命名的三阶段七节点，折叠和分页可用。
- AC-05: 项目合同检查、迁移检查和现有创建回归通过。

## 停止条件

需要真实平台写入、读取凭据、未核准官方接口合同，或无法维持 Plan-bound 单次确认时停止。

## 交付说明

按项目协议提交推送 main、重载局域网工作台；真实业务验证由本人另行确认并记录。
