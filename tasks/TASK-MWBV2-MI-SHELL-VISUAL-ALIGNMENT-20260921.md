# TASK-MWBV2-MI-SHELL-VISUAL-ALIGNMENT-20260921

任务状态、读取清单与验证结果：[Context Manifest](../tasks-context-manifests/TASK-MWBV2-MI-SHELL-VISUAL-ALIGNMENT-20260921.json)。

## 目标

将市场情报的顶栏、侧栏、对话区与输入区对齐投放执行公共工作区的实际尺寸和定位规则。

## 批准方案

用户于 2026-09-21 批准：仅修改市场情报 CSS，以投放执行当前公共工作区为唯一视觉基准；市场情报保留紫色标识、功能组件和业务内容。

## 范围

仅更新 `frontend/market-intelligence.css` 及本 Task、Manifest、项目状态和验收证据。对齐顶栏、工作区、侧栏、对话区、输入区和响应式覆盖。

## 非目标

不修改 HTML、JavaScript、服务端、接口、数据连接、月报内容或排版、颜色体系、业务逻辑及投放执行样式。

## 验收

- AC-01: 两个 Agent 在相同桌面视口下的顶栏高度、侧栏宽度、导航密度、标题起点、消息区起点和输入区高度一致。
- AC-02: 560px 窄屏下市场情报隐藏侧栏，两个 Agent 的主区与输入框均无溢出。
- AC-03: 市场情报连接配置、素材查询、月报入口和侧栏折叠保持可用；隔离浏览器回归、Git whitespace 与项目闭环检查通过。

## 停止条件

需要改变 HTML、JavaScript、接口、数据、颜色体系、月报内容或业务逻辑时停止并另立任务。

## 交付说明

交付市场情报视觉壳层的最小 CSS 对齐与浏览器尺寸核对证据。
