# TASK-MWBV2-DATABASE-DOC-CONSOLIDATION-20260908

任务状态、读取清单与验证结果：[Context Manifest](../tasks-context-manifests/TASK-MWBV2-DATABASE-DOC-CONSOLIDATION-20260908.json)。

## 目标

将数据库说明集中到数据与报表契约，删除旧最小真值说明，消除当前文档的重复定义。

## 批准方案

用户于 2026-09-08 批准“数据库文档统一归档方案”，选择数据库设计、口径和运维说明全部集中。见 [Solution Design](../docs/Solution%20Design.md#有效决策索引)。原文由 Git 基线 ec9a5764202c4e975dde1b5f4817fd3a59738d4e 保留，不创建归档副本或跳转页。

执行期间发现外部删除 `docs/plan1-新项目最高效启动框架_20260823.md` 和 `docs/plan2-最理想的逻辑图.md`；用户明确确认“保留删除并纳入本次收口”。这两处删除加入允许范围，原文同样由 Git 基线保留。

## 范围

合并经 SQL/实现核对的必要规则；将部署说明、逻辑图、Lessons 的数据库定义替换为章节引用；删除旧说明；调整 AGENTS 数据领域路由和检查器，补充运维路由回归。精确路径见 Manifest.allowed_writes。Lessons 仅整理既有内容，不新增经验。

## 非目标

不修改业务代码、SQL、数据库事实、API、平台权限、历史 Task/Manifest 或其他历史资料；不执行迁移、备份、恢复、平台调用。

## 验收

- AC-01: 当前数据契约覆盖有效字段、来源及数据库运维说明；其他当前文档不再重复数据库定义。
- AC-02: 旧文件已删除，当前入口无失效依赖，章节链接有效，原文可从 Git 基线追溯。
- AC-03: 项目合同回归通过；备份脚本及定时配置改动遗漏数据契约必读时被拒绝，补齐上下文和回写可正常关闭。
- AC-04: 启动、关闭前、关闭后检查通过；除补充批准的两份历史方案删除外，历史记录、业务实现、SQL、部署配置与 guardrails 保持原样。

## 停止条件

需要改变业务行为、无法依据当前 SQL/代码确认规则或修改超出允许路径时停止相关合并并记录缺口；禁止数据库/平台操作。

## 交付说明

数据契约已集中字段、唯一来源和数据库运维说明，旧数据库说明及补充获批的两份历史方案均已删除；AGENTS、逻辑图、Lessons、部署入口改为相应合同引用。备份脚本与定时配置已接入数据领域路由，合同回归 62 项通过。原文可由上述 Git 基线追溯，历史 Task/Manifest 和实现保持不变。在线数据库及备份/恢复未执行验证，不据此声称已完成在线对账或恢复演练。逐项验证与闭环输出见 Manifest 证据。
