# TASK-MWBV2-DEEPSEEK-SLOT-ASSIST-DIAGNOSTICS-20260914

任务状态、读取清单与验证结果：[Context Manifest](../tasks-context-manifests/TASK-MWBV2-DEEPSEEK-SLOT-ASSIST-DIAGNOSTICS-20260914.json)。

## 目标

让 DeepSeek 的连接测试与运行时槽位解析使用相同的关闭思考模式配置，并将模型辅助解析的成功或回退原因以受控信息展示在投放创建工作台。

## 批准方案

仅当 OpenAI 兼容 API Base 的主机为 `api.deepseek.com` 时，请求附加 `thinking: { "type": "disabled" }`。连接测试和运行时槽位解析复用同一请求配置器。模型调用只返回受控结果分类与已采用槽位名；不保存或展示 Key、用户原文、模型原始响应。页面显示模型辅助成功或具体安全回退原因，并在本次模型辅助失败时禁止启动。

## 范围

- OpenAI 兼容模型请求配置、DeepSeek 关闭思考模式和连接测试复用。
- 投放创建自然语言槽位解析的受控辅助结果分类与前端说明。
- 隔离 HTTP、浏览器交互和既有模型配置回归验证。
- 同步方案、当前逻辑与部署说明。

## 非目标

- 不修改 LaunchRequest v1、规则优先顺序、JSON 直校验、账户归属、Case、Job、Workflow、Plan、确认或执行链。
- 不调用真实投放平台，不读取、迁移、输出或持久化 API Key、模型原始响应或用户原文。
- 不新增模型配置开关、数据库表或跨会话草稿。

## 验收

- AC-01: DeepSeek 请求附带关闭思考模式，其他兼容供应商不附带；连接测试与运行时解析复用同一配置。
- AC-02: 超时、供应商拒绝、非 JSON、意图或置信度不合格、槽位或证据不合格均映射为受控分类，且不泄露敏感输入或原始输出。
- AC-03: 有效“巨兽战场走抖小”辅助结果显示已采用推广路线；模型辅助失败显示具体回退原因并不可启动。
- AC-04: 现有 LaunchRequest、JSON 直校验、模型配置和工作台浏览器交互回归通过。

## 停止条件

任一改动需要读取或写入真实凭据、调用真实投放平台、改变既有确认边界，或验证发现关闭思考模式不被供应商接受时停止并如实报告。

## 交付说明

完成后按项目协议提交并推送 `main`，保留用户对真实 DeepSeek 手工连接测试和新账户验证的控制权。
