# TASK-MWBV2-APPEND-VIDEO-INPUT-20260914

任务状态、读取清单与验证结果：[Context Manifest](../tasks-context-manifests/TASK-MWBV2-APPEND-VIDEO-INPUT-20260914.json)。

## 目标

修复追加视频项目推荐的展示排版，并支持带或不带标签的视频标识码列表粘贴与多行输入。

## 批准方案

用户于 2026-09-14 批准：推荐项分行显示并统一字体；在已确定追加事项、账户和项目而缺少视频时，支持逗号、顿号、分号、空白和换行分隔的无标签标识码列表；保留严格校验和模型边界。项目匹配不能覆盖解析错误。方案遵循 [Solution Design](../docs/Solution%20Design.md) 的统一 LaunchRequest 与受控 Intake 决策。

## 范围

修改自然语言 Intake 解析、服务端 Intake 回复优先级、项目推荐卡及多行输入交互；补充对话、请求解析与页面测试，并同步当前入口规则。

## 非目标

不变更数据库、项目推荐查询、平台执行、Plan 确认、回查、JSON 请求合同或模型职责。

## 验收

- AC-01: 推荐卡在桌面与窄屏分行展示名称、完整 ID、北京时间与选择按钮，无横向溢出。
- AC-02: 追加上下文中，逗号、空格、顿号、分号与换行混用的视频标识码列表可被识别。
- AC-03: 重复、非法、超限和纯数字歧义保持不可启动，项目匹配不覆盖错误。
- AC-04: 多行输入、Enter／Shift+Enter、输入法保护及页面回归通过；JSON 合同不变。

## 停止条件

需要数据库、平台写入、Plan／确认链或超出已批准输入规则的变更时停止并重新确认。

## 交付说明

完成后记录提交、测试、服务切换与页面响应证据；开发验收不表示真实追加成功。
