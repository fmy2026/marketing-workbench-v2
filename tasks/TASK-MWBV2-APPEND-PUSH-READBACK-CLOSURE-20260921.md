# TASK-MWBV2-APPEND-PUSH-READBACK-CLOSURE-20260921

任务状态、读取清单与验证结果：[Context Manifest](../tasks-context-manifests/TASK-MWBV2-APPEND-PUSH-READBACK-CLOSURE-20260921.json)。

## 目标

让已确认且仅执行一次的追加视频素材推送，在首次只读回查暂未可见时能安全、可解释地继续原 Plan 的只读回查；确认可见后才生成追加确认卡。

## 批准方案

用户已批准最小修复：保留 Case / Job、3 阶段 7 Node、现有 Gate 和确认权限；不增加后台轮询、不改 OAuth、不自动推送或追加。原 Task `TASK-MWBV2-APPEND-FLOW-CLARITY-20260921` 已取消，已发布的只读诊断保留；本 Task 承接其未完成的延迟可见回查闭环。

## 范围

- 为已消费的 `project_video_material_push` Plan 增加精确冻结视频 ID 的只读观察、真实安全证据和原 Plan 收口。
- 让恢复入口优先只读回查未收口推送；回查通过后才在当前 Job 重新准备追加确认 Plan。
- 同步数据库投影、7 Node 状态、Gate 提示和工作台对话；覆盖延迟可见、失败、并发和旧响应的隔离测试。
- 更新当前流程、方案、数据契约和部署说明，发布公司工作台，并由账户本人对原 Case 完成一次只读恢复验证。

## 非目标

- 不重复素材推送、不自动确认或追加视频，不改 OAuth、数据库 Schema、7 Node 注册表、非追加流程或全局错误框架。
- 不保存 token、secret、完整请求 URL、原始请求/响应或用户原始对话。
- 不把素材已到目标账户误写为项目追加完成。

## 验收

- AC-01: 首次 0/N、后续 N/N 的回查能收口原推送 Plan，且平台推送仅调用一次；随后才生成追加确认卡。
- AC-02: 查询失败、部分可见、身份不匹配、部分批次失败和结果不明具有准确计数、脱敏证据和不重发语义。
- AC-03: 对话、Node、底部进度和 Gate 一致；重复点击、并发回查及旧响应不产生重复写入或覆盖较新状态。
- AC-04: 回归、合同检查、发布检查通过；账户本人对原 Case 执行一次只读恢复并验证最新结果，真实追加不属于本 Task 验收。

## 停止条件

- 需要 OAuth、Schema 迁移、第二次平台推送、真实追加或非追加流程变更时停止并另行取得批准。
- 无法保持确认、Plan、动作和只读证据精确绑定，或无法安全证明状态投影一致时不发布完成声明。

## 交付说明

关闭前记录每项验收的可定位证据，执行项目合同检查，以 main 提交、推送和发布快照完成交付。原始 Case 的业务运行事实只保留在 Postgres。
