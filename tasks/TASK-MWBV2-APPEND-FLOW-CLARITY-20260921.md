# TASK-MWBV2-APPEND-FLOW-CLARITY-20260921

任务状态、读取清单与验证结果：[Context Manifest](../tasks-context-manifests/TASK-MWBV2-APPEND-FLOW-CLARITY-20260921.json)。

## 目标

使追加视频的只读准备在失败、阻断和恢复时具备一致、可操作的工作台反馈：用户能知道卡点、下一步动作，以及不会发生未确认的平台写入。

## 批准方案

用户已批准最小修复：不改变 Case / Job、3 阶段 7 Node、Plan、确认或执行边界；只区分只读查询失败与分页合同异常，保存脱敏诊断证据，将失败映射到正确追加节点，并使对话、Gate、节点与进度在同一轮运行后同步。恢复继续使用既有 Gate Action Policy。

## 范围

- 修正项目视频追加的来源库存、目标库存和项目素材只读准备的失败归类与安全诊断投影。
- 修正追加的 Node 3 / Node 4 状态、唯一 blocker 展示、对话完成消息与轮询时序。
- 为以上行为增加隔离测试，更新当前逻辑图、方案索引及发布说明，并发布公司工作台。

## 非目标

- 不修改 OAuth 刷新、平台接口、数据库 Schema、7 Node 定义、Plan/confirmation/action grant 或真实追加行为。
- 不保存 token、secret、完整 URL、原始请求、原始响应或用户原始对话。
- 不处理市场情报任务的真实联调缺口。

## 验收

- AC-01: 追加来源库存、目标库存和项目素材的首/后续页查询失败与分页合同异常可区分；Plan 仅保存脱敏诊断字段。
- AC-02: 追加失败时 Node 3、Node 4、Case Gate、对话与底部进度显示同一个实际卡点及允许的下一步。
- AC-03: 只读运行结束立即采用服务端最终 Job 投影；未确认、未消费的 Job 经恢复入口继续复用，确认与写入保护不变。
- AC-04: 隔离回归、项目合同检查与公司工作台发布检查通过；账户本人可对既有 Case 发起只读恢复，真实追加不作为本 Task 验收条件。

## 停止条件

- 需要 OAuth 修改、数据库迁移、平台写入或扩大到非追加流程时，停止并建立独立方案。
- 无法取得安全的服务端投影或测试证明时，不发布完成声明。

## 交付说明

关闭前记录每个验收的可定位证据，执行项目检查并以 main 提交、推送和发布快照完成交付。原始 Case 的实际运行状态只保留在 Postgres。
