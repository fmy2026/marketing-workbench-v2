# TASK-MWBV2-LAUNCH-REQUEST-UNIFIED-INTAKE-20260913

任务状态、读取清单与验证结果：[Context Manifest](../tasks-context-manifests/TASK-MWBV2-LAUNCH-REQUEST-UNIFIED-INTAKE-20260913.json)。

## 目标

为投放创建提供一份受限的标准请求合同，让自然语言逐步补齐和上游 JSON 在启动同一 OE3 标准项目流程前得到一致、可核对的规范化输入。

## 批准方案

按用户于 2026-09-13 批准的“投放创建统一请求与澄清机制最小方案”实施：采用 `launch-request.v1`，只接受 `create_std_project`、路线、游戏和账户；规则优先，模型仅补充输入中有依据的缺项。页面支持自然语言/JSON 切换，JSON 在页面解析后通过既有同源站内接口提交。Case 与 Job 复用同一合同；模型和 JSON 均不能代替账户归属核验、Plan 或本人确认。

## 范围

- 新增通用请求规范化与结构化 JSON 校验，并让 Intake、Case、Job 共享它。
- 增加输入模式切换、JSON 模板、草稿核对和受控澄清提示。
- 增加合成合同、HTTP 与浏览器交互回归；更新当前方案与工作台逻辑文档。
- 提交后重载本机工作台并核验入口；真实新账户验证仅准备路径和说明，不配置用户模型 Key 或确认平台动作。

## 非目标

- 不支持微信、鸿蒙、追加素材、修改 ROI、修改既有项目或其他事项。
- 不新增数据库表、跨会话草稿记忆、外部服务认证或公开 API。
- 不修改账户归属、Gate、Plan、确认、执行、回查、重试或真实平台写入规则。

## 验收

- AC-01: 自然语言与等价 JSON 生成一致的 `launch-request.v1`；JSON 不调用模型，未知字段、版本、事项、类型和混合输入均 fail-closed。
- AC-02: 分次补齐、显式纠正、多账户歧义和不支持事项产生受控澄清，不能留下可启动的陈旧草稿。
- AC-03: Case 与 Job 共享请求合同，兼容现有三字段请求，且保持 owner 核验、Case 复用和确认链路。
- AC-04: 浏览器交互验证输入模式、JSON 模板、切换清空、缺项提示、启动状态与原工作区入口；模型配置与解析来源提示保持可理解。
- AC-05: 合成回归、项目合同检查、本机重载和根入口检查通过；未配置真实模型 Key、未创建或确认任何真实平台对象。

## 停止条件

- 需要存储原始对话、原始 JSON、模型 Key，或新增跨会话草稿状态时停止。
- 需要扩大外部 API 权限、账户归属、Plan、确认、重试或平台写入范围时停止。
- 无法使 Case 与 Job 共享规范化请求且保持既有兼容行为时停止。

## 交付说明

完成后记录请求合同、页面交互、验证与新账户操作路径；动态账户、模型 Key、Case、Job 和真实平台结果不写入本 Task。
