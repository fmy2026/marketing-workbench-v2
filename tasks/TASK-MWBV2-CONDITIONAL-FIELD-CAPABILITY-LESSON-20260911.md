# TASK-MWBV2-CONDITIONAL-FIELD-CAPABILITY-LESSON-20260911

任务状态、读取清单与验证结果：[Context Manifest](../tasks-context-manifests/TASK-MWBV2-CONDITIONAL-FIELD-CAPABILITY-LESSON-20260911.json)。

## 目标

将已完成真实创建与权威回查的品牌条件字段案例，沉淀为“受控验证到通用能力收敛”的可复用经验，帮助后续面对账户能力差异时选择最小、安全的处理方式。

## 批准方案

用户已确认：一次性实验授权/临时忽略可以作为已有正式链路中的受控验证手段，但不能成为长期运行分支。成功且完成权威回查后，必须以每个 fresh Job 的目标账户能力结果收敛为通用规则，并删除运行时特批、白名单和历史候选依赖；拒绝或结果不明则保持阻断。

该工作仅更新已验证经验，不改变 [Solution Design](../docs/Solution%20Design.md) 的当前方案、运行机制或数据结构。

## 范围

- 在 `docs/project-lessons.md` 的品牌经验后补充通用决策框架。
- 建立并完成本次文档任务及 Context Manifest 闭环。

## 非目标

- 不修改 `src/`、`frontend/`、数据库、Plan、Gate、Node、路由或任何平台执行入口。
- 不查询或调用外部平台，不创建、确认、重试或修改任何业务对象。
- 不记录账户、Case、Job、凭据、原始请求或响应。

## 验收

- AC-01: 文档明确 fresh 账户能力优先、一次性受控验证的范围与审计边界、成功/拒绝/不明三种收口，以及 fail-closed 条件。
- AC-02: 文档只描述已验证、跨任务可复用的结论；不泄露动态业务或敏感信息，且项目合同检查和差异检查通过。

## 停止条件

- 新经验需要超出已验证事实的运行规则、Schema 或外部平台验证才能成立。
- 记录内容需要包含动态账户、Case、Job、凭据、原始请求或响应。

## 交付说明

完成后交付一条可定位的通用经验；不产生业务或平台副作用。
