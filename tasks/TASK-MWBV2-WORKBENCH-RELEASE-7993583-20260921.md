# TASK-MWBV2-WORKBENCH-RELEASE-7993583-20260921

任务状态、读取清单与验证结果：[Context Manifest](../tasks-context-manifests/TASK-MWBV2-WORKBENCH-RELEASE-7993583-20260921.json)。

## 目标

将已推送的 `7993583` 发布快照切换到公司共享工作台，并复核市场情报与投放执行的实际页面壳层。

## 批准方案

用户已授权执行发布与页面复核。按 [部署说明](../deploy/README.md#公司共享模式) 生成固定提交快照，以当前公司 LAN 地址重载同一 LaunchAgent；不改动源代码或业务数据。

## 范围

仅生成 `7993583` 的本地 release、重载公司共享模式的 LaunchAgent、检查根地址与浏览器页面；记录任务、清单和验证证据。

## 非目标

不修改源代码、前端样式、接口、数据库、凭据、业务配置或外部平台数据。

## 验收

- AC-01: 正在运行的 LaunchAgent 工作目录为 `7993583` 的固定 release，且公司共享根地址返回成功。
- AC-02: 浏览器实际页面中，市场情报与投放执行的顶栏高度、侧栏宽度、标题起点与输入区高度一致。
- AC-03: 任务闭环检查、差异检查和远端 SHA 核验通过。

## 停止条件

发布快照生成、服务重载、根地址检查或页面登录失败；或需要修改超出范围的文件、业务数据或凭据。

## 交付说明

已将公司共享服务切换至 `7993583`；根地址为 `http://192.168.42.7:3000`。实际服务返回的市场情报 CSS 与发布快照一致，并禁用缓存；详情见任务证据。内置浏览器连接不可用，页面视觉以实际 CSS 响应与既有 Chromium 尺寸回归复核。
