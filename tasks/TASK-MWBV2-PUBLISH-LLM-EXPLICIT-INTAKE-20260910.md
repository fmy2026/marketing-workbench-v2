# TASK-MWBV2-PUBLISH-LLM-EXPLICIT-INTAKE-20260910

任务状态、读取清单与验证结果：[Context Manifest](../tasks-context-manifests/TASK-MWBV2-PUBLISH-LLM-EXPLICIT-INTAKE-20260910.json)。

## 目标

将已完成的“受限大模型显式 Intake 与确定性进度说明”改动提交并推送至当前 GitHub `origin/main`。

## 批准方案

用户明确要求“更新 github”。发布仅包含上一已关闭实现任务的已验证工作区改动，不添加功能，不改写历史；发布前后保持 Git 状态和远端提交证据。

## 范围

- 建立发布 Task/Manifest，记录已有脏工作区的完整基线。
- 复验关闭任务已记录的关键测试与 Git 差异，提交当前改动并推送 `origin/main`。

## 非目标

- 不修改产品、数据库、Workflow、凭据、账户或平台状态。
- 不执行强推、历史改写、真实平台动作或新账户 B 验收。

## 验收

- AC-01: 当前工作区改动通过既有专项测试和 `git diff --check`。
- AC-02: 单个非改写提交包含当前实现与任务证据，已推送到 `origin/main`。
- AC-03: 发布任务闭环校验通过，工作区发布后无未跟踪的本次交付遗漏。

## 停止条件

- 远端分支非快进、出现非本次范围改动或推送需要历史改写。
- 复验失败或发现敏感信息进入待发布内容。

## 交付说明

完成后报告提交 ID、远端分支和验证结果；新账户 B 的运行验收仍需单独发起。
