# TASK-MWBV2-APPEND-VIDEO-INTAKE-20260914

任务状态、读取清单与验证结果：[Context Manifest](../tasks-context-manifests/TASK-MWBV2-APPEND-VIDEO-INTAKE-20260914.json)。

## 目标

让追加视频入口只收集账户、项目和视频标识码，并只从本人账户已验证的 Postgres 项目记录中推荐和派生路线、游戏；修复相关草稿、JSON 与输入布局问题。

## 批准方案

用户于 2026-09-14 批准“追加视频入口最小修复方案”：数据库为唯一推荐来源，不增加平台兜底；运行时 Case/Job 继续消费完整、严格校验的 `LaunchRequest v2`。依据见 [Solution Design](../docs/Solution%20Design.md) 与 [当前逻辑图](../docs/project-现在的逻辑图.md)。

## 范围

修改 Intake 解析、工作台 HTTP 接口、仓储只读查询、前端对话/JSON/布局、相关回归测试及三份当前文档；不迁移数据库。

## 非目标

不新增事项、Node、Gate 或平台执行器；不变更账户权限、Plan 确认、回查规则，不执行真实平台写入。

## 验收

- AC-01: 本人账户的已验证项目可从 Postgres 推荐，测试和越权记录不会泄露。
- AC-02: 账户、项目、视频标识码可形成完整追加请求，冲突或缺失记录不能启动。
- AC-03: 连续对话、账户切换、推荐选择和咨询行为符合受控草稿规则。
- AC-04: 简化与完整 JSON 都严格校验，编辑后旧资格失效。
- AC-05: 页面布局及现有请求、追加、对话、进度、隔离回归通过。

## 停止条件

发现需要迁移、增加业务状态、放宽本人作用域或改变 Plan/执行链时停止并重新确认。

## 交付说明

在 `main` 提交并推送；记录实际测试证据。开发验收不代表真实追加成功。
