# TASK-MWBV2-ACCOUNT-1867508116186632-40100-RECOVERY-20260910

任务状态、读取清单与验证结果：[Context Manifest](../tasks-context-manifests/TASK-MWBV2-ACCOUNT-1867508116186632-40100-RECOVERY-20260910.json)。

## 目标

在不改写旧 Case 历史、不改动代码或 Schema 的前提下，让账户 `1867508116186632` 的耗尽标准项目创建 Case 加载现行 `40100` 有界错峰投递机制，写入脱敏人工复盘批准，并由账户本人准备唯一的一次性替代 Case。

## 批准方案

用户于 2026-09-10 批准：重启实际 LAN 工作台服务以加载 commit `94e22eea` 的现行机制；对 `CASE-MWBV2-FF06CC07F45EE8F4CC` 的最新 Job 记录 `system_rate_limited` 脱敏人工复盘证据，fix version 为 `std_project_40100_redelivery_94e22eea`；随后仅由原账户 owner 在工作台启动 fresh readonly replacement。真实创建仍须由该 owner 在新冻结确认卡上输入精确短语“确认创建”。

## 范围

- 创建和维护本 Task、Manifest、项目状态及本任务脱敏验证证据。
- 重启 `com.hys.marketing-workbench.local-server`，并检查其健康状态与加载时间。
- 通过既有 `case:approve-manual-review` 受控入口写入该 Case 的脱敏 review evidence 与 metadata。
- 使用工作台既有 owner-bound replacement 流程完成 fresh readonly；若生成新 Create Plan，仅展示确认卡，不以维护入口绕过确认。

## 非目标

- 不修改源代码、数据库 Schema、payload 默认值、旧 Case 的 action、Plan、confirmation 或 readback 历史。
- 不创建第四次旧 Case action，不复制旧 Draft/Plan/hash/idempotency key，不新增第二个 replacement Case。
- 不以 CLI 或管理员身份绕过账户 owner、精确确认、Plan binding、action grant 或平台回查。
- 不记录 token、Cookie、完整 URL、raw request、raw payload 或 raw response。

## 验收

- AC-01: Task/Manifest 的启动检查通过，基线与允许写入范围准确记录。
- AC-02: LAN 工作台服务在 `94e22eea` 后重新启动并可访问，数据库保留 migration 080 的 delivery 合同。
- AC-03: 旧 Case 在零平台写入下记录脱敏 `system_rate_limited` 人工复盘证据，仍保持 3 条创建 action、零对象、零 verified readback。
- AC-04: 原账户 owner 启动 replacement 后，旧 Case 原子收口，唯一 replacement Case 固定一次逻辑 Attempt 并完成 fresh readonly。
- AC-05: 如 fresh readonly 产生 ready Create Plan，确认卡精确绑定新 Job/Plan/hash，允许一个逻辑 action 和最多三次仅限 `40100` 的物理投递；真实创建只等待 owner 的精确确认。

## 停止条件

- 当前 Case、owner、最新 Job、三次耗尽状态或零对象/verified readback 前提不再成立。
- 服务重启后未加载现行 40100 合同，或 fresh readonly 发现新的 root blocker。
- 任何路径需要修改旧历史、扩大平台写权限、绕过 owner/confirmation，或保存敏感原文。

## 交付说明

完成后记录服务加载、复盘 evidence、replacement Case/Job 与 fresh readonly 的脱敏验证结果。若新确认卡出现，保持在等待 owner 精确确认状态；不将普通“实施计划”文本视为平台创建确认。
