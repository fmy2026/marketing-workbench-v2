# TASK-MWBV2-COMPANY-WIFI-RELEASE-20260920

任务状态、读取清单与验证结果：[Context Manifest](../tasks-context-manifests/TASK-MWBV2-COMPANY-WIFI-RELEASE-20260920.json)。

## 目标

将当前 `main` 的固定发布快照部署为公司 Wi‑Fi 共享工作台，并验证唯一根地址可返回登录页。

## 批准方案

用户于 2026-09-20 明确要求“发布当前版本并切换到公司共享模式”。使用当前已分配的公司私网 IPv4 `192.168.42.7`，按部署说明生成 release 并重载既有 LaunchAgent。

## 范围

发布快照、公司共享 LaunchAgent 配置、根地址 HTTP 验证与任务证据。

## 非目标

不修改应用代码、数据库、用户、凭据或平台业务事实；不发起投放平台读写。

## 验收

- AC-01: 当前 HEAD 可生成固定 release 快照。
- AC-02: 工作台监听 `192.168.42.7:3000`，根地址返回登录页。
- AC-03: 本机与公司地址验证不触发业务写入，项目合同完成闭环。

## 停止条件

发布、共享凭据可读性、LaunchAgent 重载或根地址检查失败时，停止并保留工具的自动回滚结果。

## 交付说明

完成后同事可从唯一根地址登录；异机访问仍取决于公司 Wi‑Fi 的设备互访策略。
